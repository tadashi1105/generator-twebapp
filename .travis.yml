language: node_js
node_js:
  - '0.10'
before_install:
  - currentfolder=${PWD##*/}
  - if [ "$currentfolder" != 'generator-wptheme' ]; then cd .. && eval "mv $currentfolder generator-wptheme" && cd generator-wptheme; fi

